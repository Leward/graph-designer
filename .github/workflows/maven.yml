name: Java CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Set up JDK 1.13
      uses: actions/setup-java@v1
      with:
        java-version: 1.13
#    Test will get stuck on GitHub actions, disabled for now.
#    - name: Build and test modules with Maven
#      run: mvn -B test
    - name: Build app with Maven
      run: mvn -B javafx:jlink -pl app
